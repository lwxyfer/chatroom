let a = console.log('test a');
// export default a;

var b = console.log('test b')

// 需要理解模块化   详细内容, 模块化的实践，  别处只要有这个文件的导入，那么就会执行者里面的函数，而且没有export也不会出问题，也会执行函数。
// 所以也将模块化理解为 script标签的连接。并不是我初始的连接。
